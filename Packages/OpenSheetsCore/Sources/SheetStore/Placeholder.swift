/// Reserved for A6 — file watcher, sync state machine, diff, snapshots, workspace grants, GRDB.
///
/// Wave 0 only creates the target so everything downstream compiles. Delete this file once
/// there is real code in here.
public enum SheetStorePlaceholder {}
