/// Reserved for A7 — `WorkbookBuilder`, `SyntheticWorkbook`, the fakes, the matchers, and the
/// SwiftUI snapshot helper.
///
/// Wave 0 only creates the target so everything downstream compiles. Delete this file once
/// there is real code in here.
public enum TestSupportPlaceholder {}
